[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  SECRETS_SCAN_OMIT_KEYS = "VITE_ADMIN_EMAIL,VITE_ADMIN_PASSWORD"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
